[package]
name = "macros"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["proc-macro"]

[dependencies]
quote = "1.0.45"
syn = { version = "2.0.117", features = ["extra-traits"] }
traits = { version = "0.1.0", path = "../traits" }
